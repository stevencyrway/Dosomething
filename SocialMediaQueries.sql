
Select id,
       account_id,
       created_at,
       full_text,
       truncated,
       source,
       in_reply_to_status_id,
       in_reply_to_user_id,
       in_reply_to_screen_name,
       retweet_count,
       favorite_count,
       favorited,
       retweeted,
       lang,
       tweet_type,
       media_key,
       card_uri,
       geo_type,
       geo_coordinates,
       coordinates_type,
       coordinates_coordinates,
       followers,
       user_id
from twitter.tweet;

Select * from tiktok_ads.campaign_report_daily;

Select * from tiktok_ads.ad_report_daily
